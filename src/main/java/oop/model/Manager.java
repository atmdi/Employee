package oop.model;

public class Manager extends Employee {
        public Manager(String nationalId, String name, double salary, double taxRate) {
            super(nationalId, name, salary, taxRate);
        }
        public Manager()
        {}

    }
